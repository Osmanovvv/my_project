/**
 * Публичные контакты и данные организации.
 *
 * Единственный источник правды: страница `/contacts`, футер и микроразметка
 * Organization читают отсюда.
 *
 * ПРАВИЛО: в `CONTACT_CHANNELS` попадает только то, что реально работает.
 * Не заведён ящик — нет строки. Нет рабочего номера — нет строки.
 * Раньше здесь лежали заглушки с флагом `placeholder`, и их приходилось
 * фильтровать перед каждой выдачей наружу; пустой список надёжнее флага —
 * показать несуществующий контакт теперь физически нечем.
 *
 * Пока список пуст, единственный способ связи — форма заявки. Страница
 * контактов это учитывает и не рисует пустую сетку.
 */

export type ContactChannel = {
  id: "telegram" | "phone" | "email";
  label: string;
  /** Что видит пользователь. */
  value: string;
  href: string;
};

/**
 * Бот, который принимает заявки с формы, — подтверждённый и наш.
 * Публичным контактом не является: в него пишет сервер, а не посетитель.
 * Ник нужен, чтобы не потерять, к какому боту относятся `TELEGRAM_*`
 * в переменных окружения.
 */
export const TELEGRAM_BOT_USERNAME = "ITagent_bot";

export const ORGANIZATION = {
  name: "IT-Agent",
  legalName: "IT-Agent",
  description:
    "Студия, которая собирает сайты, Telegram-боты и админку заявок в одну систему: заявка приходит менеджеру моментально и не теряется.",
  /** Города/страна для микроразметки. */
  areaServed: "RU",
  language: "ru",
} as const;

/**
 * Пусто до запуска: рабочий номер не заведён, почта на домене не поднята,
 * а личный аккаунт в Telegram публиковать пока нечего.
 *
 * Как заполнять: добавить строку — и канал сам появится на `/contacts`
 * и в микроразметке. Ничего больше править не нужно.
 */
export const CONTACT_CHANNELS: ContactChannel[] = [];
